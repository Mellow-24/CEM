/**
 * Reply-language selector, Node half. The empty apply keeps the browser-only
 * feature in the Loader roster; the Host response-language domain owns the
 * logged preference, resolution, prompt section, command, and projection.
 */

/** Host plugin body — this package contributes only a browser surface. */
export function apply(): void {}
