/** Host loader entry for the CEM customer portal browser implementation exported from `./client`. */

/** Host plugin body — this package contributes browser UI only. */
export function apply(): void {}
